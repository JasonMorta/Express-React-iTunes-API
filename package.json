{
  "name": "capstone-project-ii",
  "version": "1.0.0",
  "description": "Get media from itunes",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "nodemon app.js",
    "heroku-postbuild": "NPM_CONFIG_PRODUCTION=false npm install --prefix frontend && npm run build --prefix frontend"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.20.0",
    "express": "^4.18.1",
    "helmet": "^5.1.0",
    "isomorphic-fetch": "^3.0.0",
    "nodemon": "^2.0.16",
    "request": "^2.88.2"
  },
  "engines": {
    "node": "16.14.2",
    "npm": "8.5.0"
  },
  "devDependencies": {
    "chai": "^4.3.6",
    "mocha": "^10.0.0"
  }
}
